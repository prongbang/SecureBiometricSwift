import XCTest
@testable import SecureBiometricSwift

final class SecureBiometricSwiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SecureBiometricSwift().text, "Hello, World!")
    }
}
